# Backup and Restore

The current implementation supports:

- copying the local database to a user-chosen path
- restoring the database from a chosen path
- audit entries for backup and restore events

Next hardening step:

- replace raw copy backups with encrypted archive backups
- add backup retention and restore verification workflow

